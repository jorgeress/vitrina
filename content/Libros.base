formulas:
  # `nota` esta vacia en casi la mitad de las fichas, y el comparador de Bases
  # manda los vacios al principio cuando el orden es DESC: la galeria abria con
  # lo que no he puntuado. Esto los baja a -1 para que caigan al final.
  puntuacion: if(note.nota, note.nota, -1)
  # La nota que se pinta, que no es la que se ordena. Falta en 47 de las 90
  # fichas, y en una rejilla eso se nota: las tarjetas sin ella median una fila
  # menos y las filas de la galeria quedaban dentadas. El guion las iguala, y
  # dice ademas lo que es verdad -- que no la he puntuado -- con el mismo signo
  # que ya usan las tablas para una casilla vacia.
  nota: if(note.nota, note.nota, "—")
filters:
  and:
    - note.tipo == "libro"
properties:
  file.name:
    displayName: Título
  note.year:
    displayName: Año
  note.autor:
    displayName: Autor
  note.nota:
    displayName: Nota
views:
  - type: cards
    name: Galería
    order:
      - file.name
      - note.autor
      - note.year
      - formula.nota
    sort:
      - property: formula.puntuacion
        direction: DESC
      - property: file.name
        direction: ASC
    image: note.portada
    cardSize: 220
    imageAspectRatio: 0.67
  - type: table
    name: Tabla
    order:
      - file.name
      - note.autor
      - note.year
      - note.nota
    sort:
      - property: formula.puntuacion
        direction: DESC
      - property: file.name
        direction: ASC
  - type: cards
    name: Favoritos
    filters:
      and:
        - note.favorito == true
    order:
      - file.name
      - note.autor
      - note.year
      - formula.nota
    sort:
      - property: formula.puntuacion
        direction: DESC
      - property: file.name
        direction: ASC
    image: note.portada
    cardSize: 220
    imageAspectRatio: 0.67
  # No hay vista de lo ya leído: Galería ya los enseña, y con casi todo
  # leído seria la misma lista con otro nombre. Lo que si hace falta
  # apartado es lo que queda, que es lo que esta pestaña enseña.
  # "en curso" entra aqui: empezado no es terminado, y si se quedara fuera, el
  # que estoy leyendo ahora no saldria en ninguna de las dos. "abandonado" no,
  # que eso ya no espera nada.
  #
  # Y sin nota, ni pintada ni en el orden: no la tiene nada de lo que todavia no
  # has leido, asi que la fila serian guiones y el orden un empate a -1 que
  # acabaria resolviendo el titulo igual. Se ordena por titulo directamente.
  - type: cards
    name: Por leer
    filters:
      or:
        - note.estado == "pendiente"
        - note.estado == "en curso"
    order:
      - file.name
      - note.autor
      - note.year
    sort:
      - property: file.name
        direction: ASC
    image: note.portada
    cardSize: 220
    imageAspectRatio: 0.67
